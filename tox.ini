[tox]
envlist = py3
isolated_build = True

[testenv]
deps =
    matplotlib
    pykry
    meshzoo >= 0.7.0, < 0.8.0
    pyamg
    netCDF4
    pytest
    pytest-codeblocks
    pytest-cov
commands =
    pytest {posargs} --codeblocks
